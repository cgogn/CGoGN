
qt_wrap_ui( PLUGIN_UIC_FILES ${PLUGIN_UI_FILES} )

qt_wrap_cpp( PLUGIN_MOC_FILES ${PLUGIN_QOBJECT_FILES} )

if(APPLE)
	set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wno-deprecated")	
endif()

ADD_LIBRARY( ${PLUGIN_NAME} SHARED
	${PLUGIN_FILES}
	${PLUGIN_UIC_FILES}
	${PLUGIN_MOC_FILES}
)

TARGET_LINK_LIBRARIES( ${PLUGIN_NAME}
	SCHNAppsLib
	${CGoGN_LIBS}
	${EXT_LIBS}
)

qt_use_modules(${PLUGIN_NAME} Core OpenGL Xml Svg Gui)


